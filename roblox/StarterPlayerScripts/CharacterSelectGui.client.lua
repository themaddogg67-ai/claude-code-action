--[[
	CharacterSelectGui  (LocalScript)  — SUPERSEDED

	The standalone character-select screen was replaced by CampaignMenu
	(StarterPlayerScripts > CampaignMenu), which runs the full
	Campaign → Season Select → Character Select flow. Character picking now lives
	inside that menu.

	This file is intentionally a no-op so an old copy in the place doesn't put a
	second character-select GUI on screen. You can safely delete this script;
	the CharacterSelect (server) morph logic is unchanged and still used.
]]

return
